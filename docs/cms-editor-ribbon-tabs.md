# CMS editor ribbon

The CMS rich-text editor keeps the existing editing controls and presents them in three distinct ribbon tabs: **Home**, **Insert**, and **View**. On mobile, each tab's tool rail remains a single horizontally scrollable row so controls never wrap into a crowded multi-row toolbar.

The CMS media action supports multiple image/video uploads in one picker, and inserted images retain the selection-based resize interaction.
