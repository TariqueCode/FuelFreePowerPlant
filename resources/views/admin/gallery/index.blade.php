@extends('layouts.portal')
@section('title','Gallery Management')
@section('content')
<section class="hero"><div><span class="eyebrow">MEDIA MANAGEMENT</span><h1>Gallery Management</h1><p>Manage photo and video galleries with dedicated covers, descriptions and automatic URLs.</p></div><a class="primary" href="{{ route('admin.gallery.create') }}"><i class="fa-solid fa-plus"></i> New Gallery</a></section>
@if(session('status'))<div class="notice">{{ session('status') }}</div>@endif
<div class="grid">@forelse($galleries as $gallery)<article class="gallery-card"><a href="{{ route('admin.gallery.edit',$gallery) }}" class="cover">@if($gallery->image_path)<img src="{{ asset('storage/'.$gallery->image_path) }}" alt="{{ $gallery->title }}">@else<div class="no-cover"><i class="fa-regular fa-images"></i><span>No cover photo</span></div>@endif</a><div class="body"><div class="meta"><span class="status {{ $gallery->status }}">{{ $gallery->status }}</span><span>{{ $gallery->gallery_media_count }} media</span></div><h2>{{ $gallery->title }}</h2>@if($gallery->excerpt)<p>{{ $gallery->excerpt }}</p>@endif<div class="actions"><a href="{{ route('admin.gallery.edit',$gallery) }}"><i class="fa-solid fa-pen"></i> Edit</a><form method="POST" action="{{ route('admin.gallery.destroy',$gallery) }}" onsubmit="return confirm('Delete this gallery and all its media?')">@csrf @method('DELETE')<button><i class="fa-solid fa-trash"></i> Delete</button></form></div></div></article>@empty<div class="empty">No galleries yet.<a href="{{ route('admin.gallery.create') }}">Create your first gallery</a></div>@endforelse</div>{{ $galleries->links() }}
@endsection
@push('styles')<style>.hero{display:flex;justify-content:space-between;align-items:center;gap:18px;margin-bottom:18px}.eyebrow{font-size:9px;letter-spacing:.16em;color:#54cde8}.hero h1{margin:6px 0;font-size:clamp(26px,4vw,40px)}.hero p{color:#7898a5;font-size:11px;line-height:1.7;max-width:680px}.primary{display:inline-flex;align-items:center;gap:8px;padding:11px 15px;border-radius:11px;background:#28aaca;color:white;text-decoration:none;font-size:10px;font-weight:800;white-space:nowrap}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:15px}.gallery-card{overflow:hidden;border:1px solid var(--line);border-radius:17px;background:rgba(255,255,255,.025)}.cover{display:block;height:210px;background:#061923}.cover img{width:100%;height:100%;object-fit:cover}.no-cover{height:100%;display:grid;place-items:center;color:#6f909d;font-size:10px;gap:5px}.no-cover i{font-size:30px}.body{padding:16px}.meta{display:flex;justify-content:space-between;align-items:center;color:#6f909d;font-size:9px}.status{padding:4px 7px;border-radius:999px;text-transform:uppercase;font-size:7px}.published{background:rgba(32,212,170,.1);color:#72e5c9}.draft{background:rgba(255,190,70,.08);color:#e6c37c}.body h2{margin:9px 0 5px;font-size:15px}.body p{margin:0;color:#7898a5;font-size:10px;line-height:1.65;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}.actions{display:flex;gap:7px;margin-top:14px}.actions a,.actions button{border:1px solid var(--line);border-radius:9px;background:transparent;color:#9bbac3;padding:8px 10px;font-size:9px;text-decoration:none}.actions form{margin:0}.empty{grid-column:1/-1;min-height:250px;border:1px dashed var(--line);border-radius:17px;display:flex;flex-direction:column;align-items:center;justify-content:center;color:#6f909d;gap:10px}.empty a{color:#5fd4ed;text-decoration:none}@media(max-width:850px){.grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:650px){.hero{align-items:flex-start;flex-direction:column}.primary{width:100%;justify-content:center}.grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.cover{height:150px}.body{padding:11px}.body h2{font-size:12px}.actions a,.actions button{padding:7px;font-size:8px}} </style>@endpush
