@extends('layouts.portal')
@section('title','Career Applications')
@section('content')
<section class="mail-hero"><div><span class="eyebrow">CAREER</span><h1>Applications</h1><p>Review CVs and candidate information submitted through the public Career page.</p></div><div class="mail-count"><strong>{{ $applications->total() }}</strong><span>applications</span></div></section>
<div class="filter-row"><a class="{{ !request('status')?'active':'' }}" href="{{ route('admin.career-applications.index') }}">All</a>@foreach(['new','reviewing','shortlisted','rejected','hired'] as $status)<a class="{{ request('status')===$status?'active':'' }}" href="{{ route('admin.career-applications.index',['status'=>$status]) }}">{{ ucfirst($status) }}</a>@endforeach</div>
<section class="application-list">@forelse($applications as $application)<a class="application-row" href="{{ route('admin.career-applications.show',$application) }}"><div class="avatar"><i class="fa-solid fa-user"></i></div><div class="candidate"><strong>{{ $application->name }}</strong><span>{{ $application->email }} @if($application->position) · {{ $application->position }} @endif</span></div><span class="status {{ $application->status }}">{{ ucfirst($application->status) }}</span><time>{{ $application->created_at->format('d M Y') }}</time></a>@empty<div class="empty">No career applications yet.</div>@endforelse</section>
{{ $applications->links() }}
@endsection
@push('styles')<style>.mail-hero{display:flex;justify-content:space-between;gap:18px;align-items:flex-end;padding:6px 0 20px}.mail-hero h1{font-size:clamp(30px,5vw,48px);margin:8px 0}.mail-hero p{color:#7898a5;max-width:650px;font-size:11px;line-height:1.7}.mail-count{min-width:110px;padding:14px;border:1px solid var(--line);border-radius:16px;text-align:center}.mail-count strong{display:block;font-size:24px}.mail-count span{color:#7898a5;font-size:9px}.filter-row{display:flex;gap:7px;overflow:auto;margin-bottom:12px}.filter-row a{padding:8px 12px;border:1px solid var(--line);border-radius:999px;color:#7898a5;text-decoration:none;font-size:10px;white-space:nowrap}.filter-row a.active{color:#eaf8fb;background:rgba(67,194,229,.1)}.application-list{border:1px solid var(--line);border-radius:18px;overflow:hidden;background:rgba(5,22,32,.72)}.application-row{display:flex;align-items:center;gap:12px;padding:14px;border-bottom:1px solid rgba(255,255,255,.05);text-decoration:none;color:inherit}.application-row:last-child{border-bottom:0}.avatar{width:38px;height:38px;display:grid;place-items:center;border-radius:12px;background:rgba(67,194,229,.08);color:#6bd7ef}.candidate{min-width:0;flex:1}.candidate strong,.candidate span{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.candidate strong{font-size:12px}.candidate span{color:#718f9c;font-size:9px;margin-top:3px}.status{font-size:8px;padding:5px 8px;border-radius:999px;background:rgba(255,255,255,.06);color:#a9c0c8}.status.new{color:#7fe7f5}.status.shortlisted{color:#8ee8c6}.status.rejected{color:#f2a3a3}.status.hired{color:#a9e5a8}time{font-size:8px;color:#617f8b;white-space:nowrap}.empty{padding:40px;text-align:center;color:#7898a5}@media(max-width:600px){.mail-hero{align-items:flex-start}.application-row{gap:9px}.application-row time{display:none}.status{margin-left:auto}.candidate span{max-width:180px}}</style>@endpush