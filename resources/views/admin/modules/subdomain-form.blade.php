@extends('layouts.portal')
@section('title','Add Subdomain')
@section('content')
<section class="hero"><div class="eyebrow">INFRASTRUCTURE</div><h1>Add subdomain</h1><p>Register the intended hostname and target. DNS records must still be created at the authoritative DNS provider.</p></section>
@if($errors->any())<div class="errors">{{ $errors->first() }}</div>@endif
<div class="form-card"><form method="POST" action="{{ route('admin.subdomains.store') }}">@csrf<div class="fields"><div><label>Owner</label><select name="user_id" required>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }} — {{ $user->email }}</option>@endforeach</select></div><div><label>Hostname</label><input name="name" value="{{ old('name') }}" placeholder="portal.example.com" required></div><div class="full"><label>Target</label><input name="target" value="{{ old('target') }}" placeholder="192.0.2.10 or https://service.example.com"></div><div><label>Status</label><select name="status"><option value="active">Active</option><option value="suspended">Suspended</option></select></div><div class="check"><input id="ssl" type="checkbox" name="ssl_enabled" value="1" checked><label for="ssl">SSL enabled</label></div></div><div class="actions"><a href="{{ route('admin.subdomains') }}">Cancel</a><button type="submit">Save subdomain</button></div></form></div>
@endsection
@push('styles')<style>.form-card{max-width:850px;background:rgba(255,255,255,.025);border:1px solid var(--line);border-radius:18px;padding:22px}.fields{display:grid;grid-template-columns:1fr 1fr;gap:16px}.full{grid-column:1/-1}label{display:block;font-size:12px;color:#9eb9c4;margin:0 0 7px}input,select{width:100%;box-sizing:border-box;padding:13px;border-radius:11px;border:1px solid var(--line);background:#071c29;color:#e9f7fb;outline:none}.check{display:flex;align-items:center;gap:8px}.check input{width:auto}.check label{margin:0}.errors{margin-bottom:16px;padding:11px;border-radius:10px;background:rgba(210,65,65,.12);color:#ffb0b0}.actions{display:flex;justify-content:flex-end;gap:12px;margin-top:22px}.actions a{color:#8ca9b6;text-decoration:none}.actions button{border:0;border-radius:11px;padding:12px 17px;background:#31afd2;color:#fff;font-weight:700}@media(max-width:650px){.fields{grid-template-columns:1fr}.full{grid-column:auto}}</style>@endpush
