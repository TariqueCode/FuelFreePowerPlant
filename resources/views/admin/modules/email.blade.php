@extends('layouts.portal')
@section('title','Email Management')
@section('content')
<section class="hero"><div class="eyebrow">MANAGED EMAIL SERVICES</div><h1>Email</h1><p>Create and manage hosting mailboxes, then connect them from Android, Windows, Linux, Outlook, Thunderbird or any standard IMAP/SMTP client.</p></section>
@if(session('status'))<div class="notice">{{ session('status') }}</div>@endif
@if($errors->any())<div class="errors">{{ $errors->first() }}</div>@endif
<div class="toolbar">@if(auth()->user()->hasPermission('email.manage'))<a class="action" href="{{ route('admin.email.create') }}">＋ Create mailbox</a>@endif</div>
<div class="table-card"><div class="table-wrap"><table><thead><tr><th>Mailbox</th><th>Owner</th><th>Provisioning</th><th>Status</th><th>Connection</th><th>Actions</th></tr></thead><tbody>@forelse($accounts as $account)<tr><td><strong>{{ $account->address }}</strong><small>{{ $account->display_name ?: 'No display name' }}</small></td><td>{{ $account->user->name }}</td><td><span class="badge {{ $account->provisioned ? 'ok' : 'pending' }}">{{ $account->provisioned ? 'Created on server' : 'Record only' }}</span><small>{{ $account->provider_message }}</small></td><td>{{ ucfirst($account->status) }}</td><td><div><b>IMAP</b> {{ $account->imap_host ?: '—' }}:{{ $account->imap_port }}</div><div><b>SMTP</b> {{ $account->smtp_host ?: '—' }}:{{ $account->smtp_port }}</div><small>Username: {{ $account->username ?: $account->address }}</small></td><td>@if(auth()->user()->hasPermission('email.manage'))<form method="POST" action="{{ route('admin.email.destroy',$account) }}" onsubmit="return confirm('This will remove the mailbox from the hosting server and delete its local record. Continue?')">@csrf @method('DELETE')<button class="danger">Delete mailbox</button></form>@endif</td></tr>@empty<tr><td colspan="6">No mailboxes yet. Create your first mailbox from the button above.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $accounts->links() }}</div></div>
@endsection
@push('styles')<style>.toolbar{display:flex;justify-content:flex-end;margin-bottom:14px}.action{padding:11px 15px;border-radius:11px;background:#31afd2;color:#fff;text-decoration:none;font-weight:700;font-size:13px}.notice,.errors{padding:12px 14px;border-radius:12px;margin-bottom:14px}.notice{background:rgba(67,194,137,.1);border:1px solid rgba(67,194,137,.2);color:#a8e5ca}.errors{background:rgba(210,65,65,.12);color:#ffb0b0}.table-card{background:rgba(255,255,255,.025);border:1px solid var(--line);border-radius:18px;overflow:hidden}.table-wrap{overflow-x:auto}table{width:100%;border-collapse:collapse;min-width:980px}th,td{text-align:left;padding:14px 16px;border-bottom:1px solid rgba(255,255,255,.06);font-size:12px;vertical-align:top}th{color:#74cce9;font-size:10px;text-transform:uppercase;letter-spacing:.08em}td{color:#b5cbd4}td small{display:block;color:#718f9d;margin-top:4px;line-height:1.5}.badge{display:inline-block;padding:5px 8px;border-radius:999px;font-size:10px}.badge.ok{background:rgba(67,194,137,.12);color:#8fe1bb}.badge.pending{background:rgba(230,170,55,.12);color:#f0ce8a}.danger{border:0;background:transparent;color:#ff9eaa;cursor:pointer;padding:0;font:inherit}.pagination{padding:12px}</style>@endpush
