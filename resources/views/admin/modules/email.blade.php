@extends('layouts.portal')
@section('title','Email Management')
@section('content')
<section class="hero"><div class="eyebrow">ADMIN MAIL SERVICES</div><h1>Mail Accounts</h1><p>Manage all <b>@{{ config('cpanel.domain','fuelfreepowerplant.com') }}</b> employee mailboxes from the main admin dashboard. Mailbox login and personal settings live separately at the Webmail portal.</p></section>
@if(session('status'))<div class="notice">{{ session('status') }}</div>@endif
@if($errors->any())<div class="errors">{{ $errors->first() }}</div>@endif
<div class="toolbar"><a class="webmail-action" href="{{ config('cpanel.webmail_url','https://mail.fuelfreepowerplant.com') }}" target="_blank" rel="noopener"><i class="fa-solid fa-envelope-open-text"></i> Open Webmail</a>@if(auth()->user()->hasPermission('email.manage'))<a class="action" href="{{ route('admin.email.create') }}"><i class="fa-solid fa-plus"></i> Create Email</a>@endif</div>
<div class="mail-note"><i class="fa-solid fa-shield-halved"></i><span><strong>Separated mail service:</strong> employees do not need website admin access. They use their full email address and mailbox password at <b>mail.fuelfreepowerplant.com</b>. The main website admin account is never used as a mailbox.</span></div>
<div class="account-grid">
@forelse($accounts as $account)
<article class="account-card">
<div class="account-head"><div><div class="address"><i class="fa-solid fa-at"></i>{{ $account->address }}</div><div class="display">{{ $account->display_name ?: 'No display name' }}</div></div><span class="status {{ $account->status === 'active' ? 'active' : 'inactive' }}">{{ $account->status === 'active' ? 'Active' : 'Inactive' }}</span></div>
<div class="meta"><span><i class="fa-solid fa-server"></i>{{ $account->provisioned ? 'Hosting mailbox' : 'Local record only' }}</span><span><i class="fa-solid fa-infinity"></i>{{ config('cpanel.quota',0) == 0 ? 'Unlimited quota' : config('cpanel.quota').' MB quota' }}</span></div>
@if(auth()->user()->hasPermission('email.manage'))
<div class="account-actions">
<form method="POST" action="{{ route('admin.email.password',$account) }}" class="password-form">@csrf<input type="password" name="password" placeholder="New password" minlength="10" required><input type="password" name="password_confirmation" placeholder="Confirm" minlength="10" required><button class="secondary" type="submit"><i class="fa-solid fa-key"></i> Change password</button></form>
<form method="POST" action="{{ route('admin.email.toggle',$account) }}">@csrf<button class="{{ $account->status === 'active' ? 'warning-btn' : 'success-btn' }}" type="submit"><i class="fa-solid {{ $account->status === 'active' ? 'fa-pause' : 'fa-play' }}"></i>{{ $account->status === 'active' ? 'Deactivate' : 'Activate' }}</button></form>
<form method="POST" action="{{ route('admin.email.destroy',$account) }}" onsubmit="return confirm('Delete {{ $account->address }} from the hosting mail server? This can permanently remove the mailbox and its messages.')">@csrf @method('DELETE')<button class="danger" type="submit"><i class="fa-solid fa-trash-can"></i> Delete</button></form>
</div>
@endif
</article>
@empty<div class="empty"><i class="fa-regular fa-envelope"></i><h3>No mailboxes yet</h3><p>Create the first @{{ config('cpanel.domain','fuelfreepowerplant.com') }} employee mailbox.</p></div>@endforelse
</div><div class="pagination">{{ $accounts->links() }}</div>
@endsection
@push('styles')<style>.toolbar{display:flex;justify-content:flex-end;align-items:center;gap:9px;flex-wrap:wrap;margin-bottom:14px}.action,.webmail-action{display:inline-flex;align-items:center;gap:8px;padding:11px 15px;border-radius:11px;color:#fff;text-decoration:none;font-weight:700;font-size:13px}.action{background:#31afd2}.webmail-action{background:rgba(69,200,232,.08);border:1px solid rgba(69,200,232,.2);color:#9fe9fa}.notice,.errors,.mail-note{padding:12px 14px;border-radius:12px;margin-bottom:14px}.notice{background:rgba(67,194,137,.1);border:1px solid rgba(67,194,137,.2);color:#a8e5ca}.errors{background:rgba(210,65,65,.12);color:#ffb0b0}.mail-note{display:flex;gap:10px;align-items:flex-start;background:rgba(69,200,232,.045);border:1px solid rgba(69,200,232,.12);color:#91b3be;font-size:12px;line-height:1.6}.mail-note i{color:#58d4ee;margin-top:2px}.mail-note strong,.mail-note b{color:#d8f7fc}.account-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.account-card{background:rgba(255,255,255,.025);border:1px solid var(--line);border-radius:18px;padding:18px}.account-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.address{font-size:16px;font-weight:800;color:#eaf8fb;word-break:break-word}.address i{color:#5bd3ee;margin-right:7px}.display{margin-top:5px;color:#718f9d;font-size:12px}.status{display:inline-flex;padding:6px 9px;border-radius:999px;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.06em}.status.active{background:rgba(67,194,137,.12);color:#8fe1bb}.status.inactive{background:rgba(230,170,55,.12);color:#f0ce8a}.meta{display:flex;gap:12px;flex-wrap:wrap;margin:15px 0;color:#7797a3;font-size:11px}.meta span{display:inline-flex;align-items:center;gap:6px}.meta i{color:#5bbfd6}.account-actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap;border-top:1px solid rgba(255,255,255,.06);padding-top:14px}.password-form{display:flex;gap:6px;flex:1;min-width:280px}.password-form input{min-width:0;flex:1;background:#071c29;color:#e9f7fb;border:1px solid var(--line);border-radius:9px;padding:9px 10px;font-size:11px}.account-actions button{border:0;border-radius:9px;padding:9px 10px;cursor:pointer;font-size:11px;font-weight:700}.secondary{background:rgba(69,200,232,.1);color:#9fe9fa}.warning-btn{background:rgba(230,170,55,.1);color:#f0ce8a}.success-btn{background:rgba(67,194,137,.1);color:#8fe1bb}.danger{background:rgba(225,75,95,.08);color:#ff9eaa}.empty{grid-column:1/-1;text-align:center;padding:55px 20px;border:1px dashed var(--line);border-radius:18px;color:#76939e}.empty i{font-size:28px;color:#55c9e4}.empty h3{color:#d9edf2;margin:12px 0 4px}.empty p{margin:0;font-size:12px}.pagination{padding:12px 0}@media(max-width:900px){.account-grid{grid-template-columns:1fr}}@media(max-width:650px){.password-form{min-width:100%;flex-direction:column}.account-actions form:not(.password-form){flex:1}.account-actions form:not(.password-form) button{width:100%}}</style>@endpush
