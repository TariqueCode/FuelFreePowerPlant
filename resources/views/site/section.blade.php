@php($siteName = $brand['name'])
<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $titles[$section] }} — {{ $siteName }}</title><style>:root{--bg:#030c12;--text:#effcff;--muted:#91aeb8;--line:rgba(93,211,238,.15);--cyan:#4fd2ee}*{box-sizing:border-box}body{margin:0;background:linear-gradient(180deg,#020a10,#061721 55%,#020a10);color:var(--text);font-family:Inter,system-ui,sans-serif}a{text-decoration:none;color:inherit}.shell{width:min(1180px,calc(100% - 28px));margin:auto}.header{position:sticky;top:0;z-index:20;background:rgba(2,10,16,.94);border-bottom:1px solid var(--line)}.nav{height:68px;display:flex;align-items:center;justify-content:space-between}.brand{font-weight:800}.menu{display:flex;gap:4px}.menu a{font-size:10px;padding:9px;color:#9bb6bf}.menu-btn{display:none}.hero{padding:60px 0 30px}.eyebrow{font-size:9px;letter-spacing:.2em;color:var(--cyan);text-transform:uppercase}.hero h1{font-size:clamp(38px,6vw,70px);margin:10px 0}.hero p{max-width:760px;color:var(--muted);line-height:1.8}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.card,.rich,.empty{border:1px solid var(--line);border-radius:18px;background:rgba(7,30,42,.72);overflow:hidden}.card img{width:100%;height:185px;object-fit:cover;display:block}.body,.rich{padding:20px}.body h2,.rich h2{margin:0 0 8px}.body p,.meta,.rich{color:var(--muted);font-size:11px;line-height:1.8}.pill{display:inline-block;margin-top:10px;padding:5px 8px;border-radius:20px;background:rgba(79,210,238,.08);color:#80d8e9;font-size:8px}.empty{padding:35px;text-align:center;color:var(--muted)}.footer{margin-top:60px;padding:30px 0;color:#688893;font-size:9px;border-top:1px solid var(--line)}.gallery-cover{height:210px!important}.gallery-date{color:#7ed9e9;font-size:9px;margin-bottom:8px}@media(max-width:760px){.menu-btn{display:block;background:none;border:1px solid var(--line);color:#fff;padding:10px}.menu{display:none;position:absolute;top:68px;left:12px;right:12px;flex-direction:column;background:#06131b;padding:10px}.menu.open{display:flex}.grid{grid-template-columns:1fr 1fr}}@media(max-width:520px){.grid{grid-template-columns:1fr}.hero{padding-top:45px}.hero h1{font-size:43px}}</style></head><body><header class="header"><div class="shell nav"><a class="brand" href="{{ route('home') }}">{{ $siteName }}</a><button class="menu-btn" type="button">☰</button><nav class="menu"><a href="{{ route('home') }}">Home</a><a href="{{ route('site.about') }}">About Us</a><a href="{{ route('site.solutions') }}">Solutions</a><a href="{{ route('site.plants') }}">Plants</a><a href="{{ route('site.future-project') }}">Future Project</a><a href="{{ route('site.gallery') }}">Gallery</a><a href="{{ route('management') }}">Management</a><a href="{{ route('site.career') }}">Career</a><a href="{{ route('contact') }}">Contact</a></nav></div></header><main class="shell"><section class="hero"><span class="eyebrow">{{ $siteName }}</span><h1>{{ $titles[$section] }}</h1><p>{{ $section === 'gallery' ? 'Events, activities, milestones and selected moments from Fuel Free Power Plant.' : ($page?->excerpt ?: $brand['tagline']) }}</p></section>
@if($page?->content)<section class="rich">{!! $page->content !!}</section>@endif
@if($section === 'about-us')
@forelse($companyItems as $item)<article class="rich">@if($item->image_path)<img src="{{ asset('storage/'.$item->image_path) }}" alt="{{ $item->title }}" style="max-width:100%;border-radius:12px">@endif<h2>{{ $item->title }}</h2>@if($item->excerpt)<p>{{ $item->excerpt }}</p>@endif{!! $item->content !!}</article>@empty<div class="empty">No company information has been published yet.</div>@endforelse
@elseif($section === 'solutions')
<section class="grid">@forelse($items as $item)<article class="card">@if($item->image_path)<img src="{{ asset('storage/'.$item->image_path) }}" alt="{{ $item->title }}">@endif<div class="body"><h2>{{ $item->title }}</h2>@if($item->excerpt)<p>{{ $item->excerpt }}</p>@endif{!! $item->content !!}</div></article>@empty<div class="empty">No solutions have been published yet.</div>@endforelse</section>
@elseif($section === 'gallery')
<section class="grid">@forelse($items as $item)<article class="card">@if($item->image_path)<img class="gallery-cover" src="{{ asset('storage/'.$item->image_path) }}" alt="{{ $item->title }}" loading="lazy">@endif<div class="body"><div class="gallery-date">{{ $item->published_at?->format('d F Y') }}</div><h2>{{ $item->title }}</h2>@if($item->excerpt)<p>{{ $item->excerpt }}</p>@endif@if($item->content)<div>{!! $item->content !!}</div>@endif</div></article>@empty<div class="empty">No gallery moments have been published yet.</div>@endforelse</section>
@elseif($section === 'plants' || $section === 'future-project')
<section class="grid">@forelse($projects as $project)<a class="card" href="{{ route('projects.show', $project->slug) }}"><div class="body"><h2>{{ $project->name }}</h2><div class="meta">{{ $project->location ?: 'Location not specified' }}<br>{{ $project->capacity_kw !== null ? number_format((float)$project->capacity_kw,1).' kW' : 'Capacity not specified' }} · {{ $project->technology ?: 'Technology not specified' }}</div><span class="pill">{{ $project->status }}</span></div></a>@empty<div class="empty">No projects have been published yet.</div>@endforelse</section>
@if($items->isNotEmpty())<section class="grid" style="margin-top:18px">@foreach($items as $item)<article class="card">@if($item->image_path)<img src="{{ asset('storage/'.$item->image_path) }}" alt="{{ $item->title }}">@endif<div class="body"><h2>{{ $item->title }}</h2><p>{{ $item->excerpt }}</p>{!! $item->content !!}</div></article>@endforeach</section>@endif
@elseif($section === 'career')
<section class="grid">@forelse($items as $item)<article class="card">@if($item->image_path)<img src="{{ asset('storage/'.$item->image_path) }}" alt="{{ $item->title }}">@endif<div class="body"><h2>{{ $item->title }}</h2><p>{{ $item->excerpt }}</p>{!! $item->content !!}</div></article>@empty<div class="empty">No career information has been published yet.</div>@endforelse</section>
@endif
</main><footer class="footer"><div class="shell">© {{ date('Y') }} {{ $siteName }} · All rights reserved.</div></footer><script>const b=document.querySelector('.menu-btn'),m=document.querySelector('.menu');b?.addEventListener('click',()=>m.classList.toggle('open'));</script></body></html>